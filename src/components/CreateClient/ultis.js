export const formatData = (clientData) => {
  debugger;
  const formattedData = {
    'name': clientData.firstName,
    'mail': clientData.mail,
    'phone': clientData.phone,
    'address': clientData.address,
  };
  debugger;
  return formattedData;
};